No arquivo original havia um código para data.trim que iria exibir mensagens de acordo com a
resposta do formulário enviado

switch (data.trim()) {
                    case "success": {
                        $("#status").html('<p class="slide-mensage success">Formulário enviado com sucesso!</p>');
                        break;
                    };
                    case "already": {
                        $("#status").html('<p class="slide-mensage alert">CPF já cadastrado!</p>');
                        break;
                    };
                    case "cpf": {
                        $("#status").html('<p class="slide-mensage alert">CPF inválido!</p>');
                        break;
                    }
                    case "teste": {
                        $("#status").html('<p class="slide-mensage sucess">Passou no Teste De</p>');
                        break;
                    }
                    default: {
                        console.log(data);
                        $("#status").html('<p class="slide-mensage alert">Ocorreu um erro ao enviar o formulário.</p>');
                    }
                }


Precisa ser trabalhado os erros de retorno e as mensagens """" !



Cada página do dashboard terá um formulário, portanto estes formulários precisam de IDs para identificar
exatamente o que será trabalhado no formulário.